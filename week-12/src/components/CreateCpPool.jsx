
export function CreateCpPool({token}) {
    const ALT_TOKEN = ""
    function createPool() {
        
    }

    return <div>
        Constant product pool
        <button onClick={createPool}>Create pool</button>
    </div>
}